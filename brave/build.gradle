dependencies {
    compile 'io.zipkin.brave:brave'
    compile 'io.zipkin.brave:brave-instrumentation-http'

    testCompile project(':thrift')
    testCompile 'io.zipkin.brave:brave-instrumentation-http-tests'
}
