// Defined in a separate module since it is possible that other tests
// may affect cyclic dependency resulting in false-negatives.
// If other tests are added to this module, developers should take care
// not to load class Flag first prior to logback initialization.
dependencies {
    implementation project(':logback')
}
