// This module is for testing ContextStorage, so we don't need other dependency except `:core`.
// To override ContextStorage, we have to use Java SPI, which affects other tests, so we need
// an isolated module.
