armeria:
  ports:
    - port: 0
    - port: 0
  # Set graceful shutdown timeout to 0 to stop the server immediately.
  graceful-shutdown-quiet-period-millis: 0

management:
  server:
    port: 0
  endpoints:
    web:
      exposure:
        include: health, loggers, prometheus
        exclude: health # exclude has a higher priority than include
