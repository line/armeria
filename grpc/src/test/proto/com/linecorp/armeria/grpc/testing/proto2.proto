syntax = "proto2";

package example.grpc.proto2;
option java_package = "example.armeria.grpc";

service Proto2Service {
  rpc simple (MessageWithEnum) returns (MessageWithEnum) {}
}

enum Foo {
  A = 1;
  B = 2;
}

message MessageWithEnum {
  optional Foo foo = 2;
}
