syntax = "proto2";

package example.grpc.proto2;
option java_package = "example.armeria.grpc";

service Proto2Service {
  rpc echo (Proto2Message) returns (Proto2Message) {}
}

enum Foo2 {
  A = 1;
  B = 2;
}

message Proto2Message {
  optional Foo2 foo = 2;
}
