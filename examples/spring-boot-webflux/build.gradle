plugins {
    alias libs.plugins.spring.boot
}

dependencies {
    implementation project(':spring:boot4-webflux-starter')

    annotationProcessor libs.spring.boot4.configuration.processor

    runtimeOnly project(':spring:boot4-actuator-starter')

    testImplementation libs.junit5.jupiter.api
    testImplementation libs.spring.boot4.starter.test
    testImplementation libs.spring.boot4.webflux.test
}
