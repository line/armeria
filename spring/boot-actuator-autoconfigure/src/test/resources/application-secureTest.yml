armeria:
  ports:
    - port: 8080
    - port: 8081
  security:
    enabled: true

management:
  server:
    port: 8082
  endpoints:
    web:
      exposure:
        include: health, loggers, prometheus
