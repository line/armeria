---
date: 2022-11-07
---

# v1.20.2

## 🛠️ Bug fixes

- You no longer see `StacklessClosedChannelException` when an incoming HTTP/1.0 or HTTP/1.1 request has
  a `Connection: close` header. #4508
- Armeria doesn't send a bad response with non-empty body anymore when a service's [ExchangeType](type) is
  [ExchangeType#UNARY](type) and the service generates non-empty body for a HEAD request. #4512
- An [HttpRequest](type) is not failed with [AbortedStreamException](type) anymore
  when an [HttpResponse](type) is correctly sent. #4494
- [DocService](type) is correctly generated for a decorated [GrpcService](type) when
  [HTTP/JSON to GRPC transcoding](https://cloud.google.com/endpoints/docs/grpc/transcoding) is enabled. #4490

{/* truncate */}

## 🙇 Thank you

<ThankYou usernames={[
  'davin111',
  'hirakida',
  'ikhoon',
  'jrhee17',
  'KarboniteKream',
  'minwoox',
  'mscheong01',
  'policeman-kh',
  'trustin'
]} />